## IDF Component Manager manifest for esp32-watch
## esp-websocket moved to external component in IDF v5.x; smartconfig is now part of esp_wifi
dependencies:
  lvgl/lvgl: ~9.5.0
  espressif/esp-websocket: ^1.4.0
  ## Required IDF version
  idf:
    version: '>=5.3.0'